<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=blog.title%></title>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
      integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <%- include("header") %>
    <div class="container">
      <div class="text-muted mb-2">
        <p>Created at: <%= blog.createdAt.toLocaleDateString() %></p>
      </div>
      <div class="card">
        <div class="card-title text-center mt-4">
          <h3><%= blog.short_desc %></h3>
        </div>
        <div class="card-text mb-4 ml-4 mr-4">
          <p><%= blog.description %></p>
        </div>
      </div>
      <div class="container mt-3">
        <a href="/" class="btn btn-dark mr-2">Back</a>
        <a href="/blog/edit/<%= blog.id%>" class="btn btn-info mr-2">Edit</a>
        <form
          action="/blog/<%= blog.id %>?_method=DELETE"
          method="POST"
          class="d-inline"
        >
          <button type="submit" class="btn btn-danger">Delete</button>
        </form>
      </div>
    </div>
    <%- include("footer") %>
  </body>
</html>
